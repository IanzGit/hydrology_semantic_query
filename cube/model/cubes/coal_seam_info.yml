cubes:
  - name: base_coal_seam_info
    sql_table: coal_seam_info
    title: "coal_seam_info"
    description: "coal_seam_info 表的基础记录；一行对应一条物理记录。"
    meta:
      business_domain: hydrology
      use_cases:
        - "查询 coal_seam_info 的基础记录和字段"
    measures:
      - name: coal_seam_info_count
        type: count
        title: "coal_seam_info记录数"
        description: "coal_seam_info 表物理记录数量，不施加默认过滤。"
    dimensions:
      - name: id
        sql: "{CUBE}.`id`"
        type: string
        primary_key: true
        public: true
        title: "id"
        description: "数据库字段 id，物理类型为 varchar。"
      - name: seam_number
        sql: "{CUBE}.`seamNumber`"
        type: string
        title: "煤层编号"
        description: "煤层编号。"
      - name: dxf_path
        sql: "{CUBE}.`dxfPath`"
        type: string
        public: false
        title: "dxf文件路径"
        description: "dxf文件路径。"
      - name: js_path
        sql: "{CUBE}.`jsPath`"
        type: string
        public: false
        title: "js文件路径"
        description: "js文件路径。"
      - name: init_color
        sql: "{CUBE}.`initColor`"
        type: string
        title: "初始颜色"
        description: "初始颜色。"
      - name: is_enabled
        sql: "{CUBE}.`isEnable`"
        type: number
        title: "是否启用"
        description: "数据库保存的启用状态原始值，不施加默认过滤。"
        meta:
          projection_role: filter_only
      - name: created_at
        sql: "{CUBE}.`createDate`"
        type: time
        title: "创建时间"
        description: "记录创建时间。"
      - name: created_by_id
        sql: "{CUBE}.`createUserId`"
        type: string
        public: false
        title: "创建用户ID"
        description: "创建记录的用户标识。"
      - name: updated_at
        sql: "{CUBE}.`updateDate`"
        type: time
        title: "更新时间"
        description: "记录最近更新时间。"
      - name: updated_by_id
        sql: "{CUBE}.`updateUserId`"
        type: string
        public: false
        title: "更新用户ID"
        description: "最近更新记录的用户标识。"
      - name: screen_ids
        sql: "{CUBE}.`screenIds`"
        type: string
        title: "适用范围"
        description: "适用范围。"
      - name: direction
        sql: "{CUBE}.`direction`"
        type: string
        title: "分组"
        description: "分组。"
      - name: sequence
        sql: "{CUBE}.`seq`"
        type: number
        title: "序号"
        description: "记录配置序号。"
