cubes:
  - name: base_data_up_device_range
    sql_table: data_up_device_range
    title: "data_up_device_range"
    description: "data_up_device_range 表的基础记录；一行对应一条物理记录。"
    meta:
      business_domain: hydrology
      use_cases:
        - "查询 data_up_device_range 的基础记录和字段"
    measures:
      - name: data_up_device_range_count
        type: count
        title: "data_up_device_range记录数"
        description: "data_up_device_range 表物理记录数量，不施加默认过滤。"
    dimensions:
      - name: id
        sql: "{CUBE}.`id`"
        type: string
        primary_key: true
        public: true
        title: "id"
        description: "数据库字段 id，物理类型为 varchar。"
      - name: point_id
        sql: "{CUBE}.`pointId`"
        type: string
        title: "pointId"
        description: "数据库字段 pointId，物理类型为 varchar。"
      - name: value_id
        sql: "{CUBE}.`valueId`"
        type: string
        title: "valueId"
        description: "数据库字段 valueId，物理类型为 varchar。"
      - name: is_enabled
        sql: "{CUBE}.`isEnable`"
        type: number
        title: "是否启用"
        description: "数据库保存的启用状态原始值，不施加默认过滤。"
        meta:
          projection_role: filter_only
      - name: created_at
        sql: "{CUBE}.`createDate`"
        type: time
        title: "创建时间"
        description: "记录创建时间。"
      - name: created_by_id
        sql: "{CUBE}.`createUserId`"
        type: string
        public: false
        title: "创建用户ID"
        description: "创建记录的用户标识。"
      - name: updated_at
        sql: "{CUBE}.`updateDate`"
        type: time
        title: "更新时间"
        description: "记录最近更新时间。"
      - name: updated_by_id
        sql: "{CUBE}.`updateUserId`"
        type: string
        public: false
        title: "更新用户ID"
        description: "最近更新记录的用户标识。"
