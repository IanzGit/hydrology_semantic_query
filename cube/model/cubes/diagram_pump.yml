cubes:
  - name: base_diagram_pump
    sql_table: diagram_pump
    title: "组态图泵房配置"
    description: "diagram_pump 表的基础记录；一行对应一条物理记录。"
    meta:
      business_domain: hydrology
      use_cases:
        - "查询 组态图泵房配置 的基础记录和字段"
    measures:
      - name: diagram_pump_count
        type: count
        title: "组态图泵房配置记录数"
        description: "diagram_pump 表物理记录数量，不施加默认过滤。"
    dimensions:
      - name: pump_id
        sql: "{CUBE}.`pumpId`"
        type: string
        primary_key: true
        public: true
        title: "pumpId"
        description: "数据库字段 pumpId，物理类型为 varchar。"
      - name: pump_name
        sql: "{CUBE}.`pumpName`"
        type: string
        title: "泵房名称"
        description: "泵房名称。"
      - name: dxf_file_id
        sql: "{CUBE}.`dxfFileId`"
        type: string
        title: "dxf文件id"
        description: "dxf文件id。"
      - name: dxf_js_path
        sql: "{CUBE}.`dxfJsPath`"
        type: string
        public: false
        title: "dxfjs路径"
        description: "dxfjs路径。"
      - name: notes
        sql: "{CUBE}.`notes`"
        type: string
        title: "备注"
        description: "备注。"
      - name: is_enabled
        sql: "{CUBE}.`isEnable`"
        type: number
        title: "是否启用"
        description: "数据库保存的启用状态原始值，不施加默认过滤。"
        meta:
          projection_role: filter_only
      - name: created_at
        sql: "{CUBE}.`createDate`"
        type: time
        title: "创建时间"
        description: "记录创建时间。"
      - name: created_by_id
        sql: "{CUBE}.`createUserId`"
        type: string
        public: false
        title: "创建用户ID"
        description: "创建记录的用户标识。"
      - name: updated_at
        sql: "{CUBE}.`updateDate`"
        type: time
        title: "更新时间"
        description: "记录最近更新时间。"
      - name: updated_by_id
        sql: "{CUBE}.`updateUserId`"
        type: string
        public: false
        title: "更新用户ID"
        description: "最近更新记录的用户标识。"
