cubes:
  - name: base_push_point_item_config
    sql_table: push_point_item_config
    title: "推送点位数据项"
    description: "push_point_item_config 表的基础记录；一行对应一条物理记录。"
    meta:
      business_domain: hydrology
      use_cases:
        - "查询 推送点位数据项 的基础记录和字段"
    measures:
      - name: push_point_item_config_count
        type: count
        title: "推送点位数据项记录数"
        description: "push_point_item_config 表物理记录数量，不施加默认过滤。"
    dimensions:
      - name: item_id
        sql: "{CUBE}.`itemId`"
        type: string
        primary_key: true
        public: true
        title: "itemId"
        description: "数据库字段 itemId，物理类型为 varchar。"
      - name: point_id
        sql: "{CUBE}.`pointId`"
        type: string
        title: "点位id"
        description: "点位id。"
      - name: config_id
        sql: "{CUBE}.`configId`"
        type: string
        title: "配置id"
        description: "配置id。"
      - name: sequence
        sql: "{CUBE}.`seq`"
        type: number
        title: "序号"
        description: "序号。"
      - name: item_name
        sql: "{CUBE}.`itemName`"
        type: string
        title: "配置项名称"
        description: "配置项名称。"
      - name: value_name
        sql: "{CUBE}.`valueName`"
        type: string
        title: "值名称"
        description: "值名称。"
      - name: value_type
        sql: "{CUBE}.`valueType`"
        type: string
        title: "值类型"
        description: "值类型。"
      - name: source_type
        sql: "{CUBE}.`sourceType`"
        type: string
        title: "来源类型"
        description: "来源类型。"
      - name: value_length
        sql: "{CUBE}.`valueLength`"
        type: number
        title: "数据长度"
        description: "数据长度。"
      - name: item_value
        sql: "{CUBE}.`itemValue`"
        type: string
        title: "配置值"
        description: "配置值。"
      - name: is_enabled
        sql: "{CUBE}.`isEnable`"
        type: number
        title: "是否启用"
        description: "数据库保存的启用状态原始值，不施加默认过滤。"
        meta:
          projection_role: filter_only
      - name: created_at
        sql: "{CUBE}.`createDate`"
        type: time
        title: "创建时间"
        description: "记录创建时间。"
      - name: created_by_id
        sql: "{CUBE}.`createUserId`"
        type: string
        public: false
        title: "创建用户ID"
        description: "创建记录的用户标识。"
      - name: updated_at
        sql: "{CUBE}.`updateDate`"
        type: time
        title: "更新时间"
        description: "记录最近更新时间。"
      - name: updated_by_id
        sql: "{CUBE}.`updateUserId`"
        type: string
        public: false
        title: "更新用户ID"
        description: "最近更新记录的用户标识。"
