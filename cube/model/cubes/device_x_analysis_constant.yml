cubes:
  - name: base_device_x_analysis_constant
    sql_table: device_x_analysis_constant
    public: true
    description: "device_x_analysis_constant 表的基础记录；一行对应一条物理记录。"
    meta:
      source_table: "device_x_analysis_constant"
      business_domain: hydrology
      aliases:
        - "device_x_analysis_constant"
      use_cases:
        - "查询 device_x_analysis_constant 的基础记录和字段"
      priority: 0.2
    measures:
      - name: device_x_analysis_constant_count
        type: count
        description: "device_x_analysis_constant 表物理记录数量，不施加默认过滤。"
    dimensions:
      - name: id
        sql: "{CUBE}.`id`"
        type: string
        primary_key: true
        public: true
        description: "数据库字段 id，物理类型为 varchar。"
      - name: analysis_id
        sql: "{CUBE}.`analysisId`"
        type: string
        description: "脚本id。"
      - name: constant_name
        sql: "{CUBE}.`constantName`"
        type: string
        description: "常量名称。"
      - name: constant_key
        sql: "{CUBE}.`constantKey`"
        type: string
        description: "常量键。"
      - name: constant_value
        sql: "{CUBE}.`constantValue`"
        type: string
        description: "常量值。"
      - name: is_edit
        sql: "{CUBE}.`isEdit`"
        type: number
        description: "是否可修改。"
      - name: is_enabled
        sql: "{CUBE}.`isEnable`"
        type: number
        description: "数据库保存的启用状态原始值，不施加默认过滤。"
        meta:
          projection_role: filter_only
      - name: created_at
        sql: "{CUBE}.`createDate`"
        type: time
        description: "记录创建时间。"
      - name: created_by_id
        sql: "{CUBE}.`createUserId`"
        type: string
        public: false
        description: "创建记录的用户标识。"
      - name: updated_at
        sql: "{CUBE}.`updateDate`"
        type: time
        description: "记录最近更新时间。"
      - name: updated_by_id
        sql: "{CUBE}.`updateUserId`"
        type: string
        public: false
        description: "最近更新记录的用户标识。"
      - name: constant_type
        sql: "{CUBE}.`constantType`"
        type: string
        description: "常量类型。"
      - name: constant_param
        sql: "{CUBE}.`constantParam`"
        type: string
        description: "常量参数。"
      - name: constant_notes
        sql: "{CUBE}.`constantNotes`"
        type: string
        description: "常量说明。"
