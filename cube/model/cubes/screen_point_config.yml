cubes:
  - name: base_screen_point_config
    sql_table: screen_point_config
    public: true
    description: "screen_point_config 表的基础记录；一行对应一条物理记录。"
    meta:
      source_table: "screen_point_config"
      business_domain: hydrology
      aliases:
        - "screen_point_config"
        - "大屏点位配置"
      use_cases:
        - "查询 大屏点位配置 的基础记录和字段"
      priority: 0.2
    measures:
      - name: screen_point_config_count
        type: count
        description: "screen_point_config 表物理记录数量，不施加默认过滤。"
    dimensions:
      - name: id
        sql: "{CUBE}.`id`"
        type: string
        primary_key: true
        public: true
        description: "数据库字段 id，物理类型为 varchar。"
      - name: sequence
        sql: "{CUBE}.`seq`"
        type: number
        description: "序号。"
      - name: point_name
        sql: "{CUBE}.`pointName`"
        type: string
        description: "点位名称。"
      - name: screen_id
        sql: "{CUBE}.`screenId`"
        type: string
        description: "大屏id。"
      - name: seam_id
        sql: "{CUBE}.`seamId`"
        type: string
        description: "煤层id。"
      - name: face_id
        sql: "{CUBE}.`faceId`"
        type: string
        description: "工作面id。"
      - name: direction
        sql: "{CUBE}.`direction`"
        type: string
        description: "分组。"
      - name: source_x
        sql: "{CUBE}.`sourceX`"
        type: string
        description: "原始坐标x。"
      - name: source_y
        sql: "{CUBE}.`sourceY`"
        type: string
        description: "原始坐标y。"
      - name: dxf_x
        sql: "{CUBE}.`dxfX`"
        type: string
        description: "dxf计算坐标x。"
      - name: dxf_y
        sql: "{CUBE}.`dxfY`"
        type: string
        description: "dxf计算坐标y。"
      - name: view_direction
        sql: "{CUBE}.`viewDirection`"
        type: string
        description: "偏移方向。"
      - name: offset_h
        sql: "{CUBE}.`offsetH`"
        type: number
        description: "偏移量。"
      - name: is_enabled
        sql: "{CUBE}.`isEnable`"
        type: number
        description: "数据库保存的启用状态原始值，不施加默认过滤。"
        meta:
          projection_role: filter_only
      - name: created_at
        sql: "{CUBE}.`createDate`"
        type: time
        description: "记录创建时间。"
      - name: created_by_id
        sql: "{CUBE}.`createUserId`"
        type: string
        public: false
        description: "创建记录的用户标识。"
      - name: updated_at
        sql: "{CUBE}.`updateDate`"
        type: time
        description: "记录最近更新时间。"
      - name: updated_by_id
        sql: "{CUBE}.`updateUserId`"
        type: string
        public: false
        description: "最近更新记录的用户标识。"
      - name: is_show
        sql: "{CUBE}.`isShow`"
        type: string
        description: "是否默认显示内容。"
