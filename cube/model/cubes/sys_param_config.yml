cubes:
  - name: base_sys_param_config
    sql_table: sys_param_config
    public: true
    description: "sys_param_config 表的基础记录；一行对应一条物理记录。"
    meta:
      source_table: "sys_param_config"
      business_domain: hydrology
      aliases:
        - "sys_param_config"
      use_cases:
        - "查询 sys_param_config 的基础记录和字段"
      priority: 0.2
    measures:
      - name: sys_param_config_count
        type: count
        description: "sys_param_config 表物理记录数量，不施加默认过滤。"
    dimensions:
      - name: id
        sql: "{CUBE}.`id`"
        type: string
        primary_key: true
        public: true
        description: "数据库字段 id，物理类型为 varchar。"
      - name: param_group
        sql: "{CUBE}.`paramGroup`"
        type: string
        description: "数据库字段 paramGroup，物理类型为 varchar。"
      - name: param_key
        sql: "{CUBE}.`paramKey`"
        type: string
        description: "数据库字段 paramKey，物理类型为 varchar。"
      - name: param_value
        sql: "{CUBE}.`paramValue`"
        type: string
        description: "数据库字段 paramValue，物理类型为 varchar。"
      - name: param_name
        sql: "{CUBE}.`paramName`"
        type: string
        description: "数据库字段 paramName，物理类型为 varchar。"
      - name: created_by_id
        sql: "{CUBE}.`createUser`"
        type: string
        description: "创建记录的用户。"
      - name: updated_by_id
        sql: "{CUBE}.`updateUser`"
        type: string
        description: "最近更新记录的用户。"
      - name: updated_at
        sql: "{CUBE}.`updateTime`"
        type: time
        description: "记录最近更新时间。"
      - name: created_at
        sql: "{CUBE}.`createTime`"
        type: time
        description: "记录创建时间。"
      - name: is_enabled
        sql: "{CUBE}.`isEnable`"
        type: number
        description: "数据库保存的启用状态原始值，不施加默认过滤。"
        meta:
          projection_role: filter_only
